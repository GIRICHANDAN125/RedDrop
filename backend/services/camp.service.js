/**
 * Camp Service — Blood Drive management business logic for RedDrop AI V2
 */
// TODO: inject camp and donor repositories
// const campRepository = require('../repositories/camp.repository');

module.exports = {
  // async list(query) { ... }
  // async create(data) { ... }
  // async getById(id) { ... }
};
